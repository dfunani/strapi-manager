'use strict';

/**
 * micro-app-country service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::micro-app-country.micro-app-country');
