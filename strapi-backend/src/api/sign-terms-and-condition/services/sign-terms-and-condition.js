'use strict';

/**
 * sign-terms-and-condition service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sign-terms-and-condition.sign-terms-and-condition');
