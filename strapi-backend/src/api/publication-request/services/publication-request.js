'use strict';

/**
 * publication-request service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publication-request.publication-request');
