'use strict';

/**
 * microapp-screenshot service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::microapp-screenshot.microapp-screenshot');
