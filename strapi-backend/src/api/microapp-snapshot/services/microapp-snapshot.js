'use strict';

/**
 * microapp-snapshot service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::microapp-snapshot.microapp-snapshot');
